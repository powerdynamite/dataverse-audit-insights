# Dataverse environment URL for local dev / standalone fallback calls.
# In a published Power Apps Code App, data goes through the Power Platform
# connectors (configured via pac code add-data-source) and this is not needed.
VITE_DATAVERSE_URL=https://org723efd4d.crm.dynamics.com
